run = "git fetch origin main && git reset --hard origin/main && cd backend && npm start"

[env]
NODE_ENV = "production"
ANTHROPIC_API_KEY = "sk-ant-demo-key"
FIREBASE_PROJECT_ID = "nomadnest-goa-demo"
FRONTEND_URL = "https://nomadnest-goa.vercel.app"
